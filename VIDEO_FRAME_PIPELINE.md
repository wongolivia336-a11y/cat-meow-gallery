# 逐帧素材流水线 · 图生视频 → 关键帧 → 桌宠动画

多米的动作帧怎么从"一张静态图"变成"会动的桌宠"。

工具：[即梦 AI](https://jimeng.jianying.com/)（图生视频，支持首帧+尾帧）

---

## 为什么走视频这条路

程序化动画（代码算腿的位置）做不出可信的动物运动 ——
猫走路时脊柱、肩胛、尾巴的联动关系太复杂，硬编码出来永远是"平移的贴图"。

而**存在感来自行为，不是渲染精度**。
预算应该花在动作上，不是毛发上。图生视频正好能给出可信的动作。

---

## 第 1 步：素材（已就绪）

`assets/domi/` 里是切干净的六帧，透明底，无邻格污染：

| 文件 | 尺寸 | 用途 |
|---|---|---|
| `sleep.png` | 461×288 | 蜷着睡 |
| `sit.png` | 405×448 | 坐着看 |
| `paw.png` | 394×432 | 舔爪 |
| `walk.png` | 490×365 | 走路 |
| `puff.png` | 396×426 | 举起泡泡棒 |
| `blow.png` | 473×388 | 吹出泡泡（含两颗泡泡） |

这些是 `scripts/extract-frames.ps1` 按连通域从
`assets/domi-sprite-sheet-v1.png` 切出来的，`frames.json` 里有每帧的 anchor。

---

## 第 2 步：生成视频

### 🫧 吹泡泡（首尾帧天然成对，优先做这个）

**首帧** `puff.png` → **尾帧** `blow.png`

```
一只银灰虎斑小猫坐着，双手举起粉色泡泡棒放到嘴边轻轻吹气，
吹出两颗透明的淡蓝色泡泡缓缓向左上方飘走，尾巴轻轻摆动。
猫的身体始终保持在画面同一位置，大小不变。
纯白色背景，固定机位，镜头完全不动，无推拉摇移。
2D手绘卡通风格，粗黑描边，平涂上色，与参考图画风配色完全一致。
```

### 🐾 走路循环

只传 `walk.png` 单帧。

```
一只银灰虎斑小猫侧面行走，四条腿交替迈步，尾巴上下轻摆。
原地行走，位置和大小完全不变。
纯白色背景，固定机位，镜头完全不动。
2D手绘卡通风格，粗黑描边，平涂上色，与参考图完全一致。
循环动画，首尾可无缝衔接。
```

### 😴 坐下睡着

**首帧** `sit.png` → **尾帧** `sleep.png`

---

## 三条硬约束（少一条就白生）

| 约束 | 为什么 |
|---|---|
| **镜头完全不动** | 镜头一动，逐帧之间猫的位置就对不齐，抽出来没法用 |
| **猫不位移、不变大小** | 同上。anchor 对齐依赖这一点 |
| **纯白背景，不要透明** | 生成模型给不出干净 alpha，白底反而好抠 |

### 参数

- **时长 3 秒**，别贪长 —— 越长漂移越大，3 秒够抽 8–12 帧
- **比例跟原图一致** —— 换比例会触发重新构图，猫会变形

---

## 第 3 步：抽帧（本机没有 ffmpeg，走浏览器）

把 mp4 放进项目目录，用 `devserver.py` 起本地服务，
然后在浏览器里用 `<video>` 定位 + canvas 截取：

1. `video.currentTime = t` 定位到关键动作点
2. 等 `seeked` 事件
3. `ctx.drawImage(video, 0, 0)` 画到 canvas
4. `canvas.toDataURL()` 拿 base64，写盘成 PNG

**按动作关键点挑，不要均匀抽。** 走路循环取"四条腿的极限位置"那几帧，
比等间隔取 12 帧有效得多 —— 稚拙风本来就不需要密集帧率。

---

## 第 4 步：切图 + 接入

```powershell
# 1. 找连通域，确认每个姿态的实际范围
./scripts/find-sprite-components.ps1

# 2. 按连通域切成独立帧 + 算 anchor
./scripts/extract-frames.ps1
```

⚠️ 抽出来的帧是**白底**，切图前要先做一次去白。
`extract-frames.ps1` 现在假设输入已经是透明底，需要加一个 keying 步骤。

然后在 `domi.js` 里：

1. `FRAME_DATA` 加上新帧（`walk-1` … `walk-N`），数值抄 `frames.json`
2. `paintWalk()` 从"整帧 + 起伏"改成按 phase 取帧播放

`paintWalk` 当前实现里已经写明了这个扩展点，**结构不用改**。

---

## 已知的坑

**1. 刚性网格切图不可靠**
原来按 3×2 网格切 1536×1024 的精灵图，但内容不老实待在格子里：
walk 的猫实际占 x 70–544（格子只到 512，尾巴被切掉），
blow 的泡泡横跨 x 998–1077（边界在 1024）。
为捞回半颗泡泡加的补丁条取 x 952–1024，
而隔壁那只猫延伸到 994 —— 把人家的黑色尾尖一起贴了过来。

**按连通域切，不要按网格切。**

**2. 黑色尾尖是故意画的**
不是抠图残留 —— 它有描边有高光。做去白/抠图时别把它当背景清掉。

**3. anchor 必须是脚底中心**
用 bbox 中心当锚点的话，换姿态时猫会上下跳（因为不同姿态的高度差很大，
sleep 只有 288px 而 sit 有 448px）。
