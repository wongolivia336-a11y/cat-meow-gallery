# 交接文档 · 桌宠「多米出场吹泡泡」

给接手这个功能的人（人或 agent）。
**先读「前置认知」再动代码** —— 这个功能有几个坑不看就会踩。

---

## 一句话目标

工作到点，多米从屏幕边缘走出来，吹一串泡泡把屏幕慢慢填满，
然后走掉，留下一屏可以戳的声音泡泡。用户戳几颗，听几声，回去工作。

**这个定时器就是产品本身** —— 它是一个休息提醒，泡泡是奖励。

---

## 前置认知（不看会踩坑）

### 1. 视觉语言：手绘，不要写实

整个产品是稚拙手绘风：暖褐墨线 `#443e37`、不对称、有笔压、米白纸底。
多米必须画成同一套语言里的角色。

> **存在感来自行为，不是渲染精度。**
> `oneko.js` 是一只 32×32 的像素猫，所有人都记得它是活的 ——
> 因为它会追鼠标、会打盹、会挠头。
> 一只写实但只会平移的猫，反而像贴图。
>
> **预算全部花在动作上，不要花在毛发上。**

参考：[oneko.js](https://github.com/adryd325/oneko.js)、[Excalidraw](https://github.com/excalidraw/excalidraw)

### 2. 性能铁律：算一次，之后只做变换

`bubbles.js` 的核心决策是**预渲染**：每颗泡泡的手绘轮廓一生只画一次
（`makeSprite`），之后每帧只做 `drawImage`。

多米的动画必须遵守同一条规则 —— **每一帧的每个姿态预渲染成贴图**，
不要每帧重跑 `rough.js` 或路径绘制。否则一边走一边吹泡泡时会掉帧。

### 3. 点击穿透是这个窗口能存在的前提

桌宠窗口是**全屏、透明、置顶**的。如果它接收点击，用户就没法用电脑了。

现在的机制（`app.js` 的 `setupDesktopPet`）：
- 默认 `setIgnoreMouseEvents(true, { forward: true })` —— 整窗穿透，但仍收 mousemove
- 每次 mousemove **和一个 160ms 的定时器**做命中检测
- 鼠标下有泡泡 → 临时关掉穿透；移开 → 立刻恢复

> ⚠️ **定时器那一半不能删。**
> 鼠标不动而泡泡自己飘走时不会有任何事件，
> 窗口会永久卡在"可交互"状态，把用户正在工作的窗口整块挡住。
>
> **多米走出来时，它自己也必须进命中检测**，否则鼠标划过多米不会有反应；
> 或者反过来 —— 如果决定多米不可点击，就必须确保它不影响穿透判定。

---

## 现有代码地图

```
bubbles.js    泡泡场。物理(Matter.js) + 预渲染(rough.js) + 每帧 drawImage
app.js        数据、录音、播放、存储、UI、桌宠穿透逻辑
electron/
  main.js     窗口、托盘、meow:// 协议、穿透 IPC
  preload.js  contextBridge 白名单
```

### 可以直接调用的接口

**`window.BubbleField`**（`bubbles.js` 末尾导出）

| 方法 | 作用 |
|---|---|
| `setItems(list)` | 全量同步当前该显示哪些泡泡，内部处理淡入淡出 |
| `setMode("ambient"\|"capture")` | 切换密度（0.46 / 0.16） |
| `setRecording(data\|null)` | 录音态；非 null 时其余泡泡整体淡出 |
| `hitTestAt(x, y)` | 该坐标下有没有泡泡（穿透判定用） |
| `faceDataUrl(mood, size)` | 导出一张多米表情图 |
| `TUNING` | 物理手感参数（弹性、阻力、限速） |

**`window.meowPet`**（`preload.js` 暴露）

| 方法 | 作用 |
|---|---|
| `isPet` | 是否运行在 Electron 桌宠里 |
| `setInteractive(bool)` | 开关整窗点击穿透 |
| `onControlMode(fn)` | 主进程通知进入/退出控制界面 |
| `exitControlMode()` | 退回穿透状态 |

---

## 要做的事

### A. 定时器（建议放主进程）

放 `electron/main.js` 而不是渲染进程，理由：
- 主进程的生命周期更稳，渲染进程被系统降频不影响计时
- 托盘菜单可以显示"距离下次休息 24 分钟"
- 以后要加"跳过这次""推迟 5 分钟"都在托盘里做

建议默认 **50 分钟工作 / 一次出场**，可配置。
出场不该打断用户 —— 它是穿透的，不抢焦点，用户可以完全无视。

新增 IPC：
```
main → renderer   "pet:showtime"       该出场了
renderer → main   "pet:showtime-done"  出场结束，回到常态
```

### B. 多米的出场动画（渲染进程，画在同一块 canvas 上）

建议拆成一个新文件 `domi.js`，和 `bubbles.js` 平级，
由 `bubbles.js` 的主循环在 `draw()` 末尾调一次 `Domi.draw(ctx, now)`，
保证多米画在泡泡之上。

序列（每一段的时长都要能单独调）：

| 阶段 | 行为 | 大约时长 |
|---|---|---|
| `walk-in` | 从屏幕左/右下角走进来，走到 1/3 处 | 2.5s |
| `settle` | 停下，坐下，甩一下尾巴 | 1s |
| `blow` × N | 每次鼓腮 → 吐出一颗泡泡 → 泡泡飘走 | 每颗 0.8s |
| `walk-out` | 走回边缘，消失 | 2s |

**吹泡泡要和真实数据挂钩**：每吹一颗，就从用户的录音里取一段，
调 `BubbleField` 让那一颗进场。不要吹出空泡泡 —— 泡泡必须有声音。

> 需要给 `BubbleField` 加一个新接口，比如
> `spawnAt(item, x, y, velocity)`，让泡泡从多米嘴边生出来并带初速度。
> 现在的 `spawn()` 是随机位置，不满足需求。

### C. 密度渐进

现在 `setItems()` 会一次性把泡泡补到目标密度。
出场序列需要「从空到满」的过程：

1. 出场开始前：`BubbleField` 清空或淡出现有泡泡
2. 每吹一颗，密度 +1
3. 吹满后恢复常态

建议给 `bubbles.js` 加 `setTargetCount(n)`，
让 `targetCount()` 在有显式值时优先用它。

### D. 多米的贴图

**每个姿态预渲染一次，缓存起来。** 需要的姿态：

```
walk-1 / walk-2 / walk-3 / walk-4   走路循环（4 帧够了，稚拙风不需要更多）
sit                                  坐下
puff                                 鼓腮（吹之前）
blow                                 吐泡泡的瞬间
```

画法沿用 `bubbles.js` 里已有的手绘笔触工具（它们已经导出在模块内部，
需要的话把 `makeRng` / `jitterLine` / `inkStroke` 提升为共享工具）：

- `makeRng(seed)` —— 有种子的随机，保证同一姿态每次一样
- `jitterLine(x1,y1,x2,y2,rng,amp,steps)` —— 线条中段抖动，端点不抖
- `inkStroke(c, pts, rng, width)` —— 分段描边做出笔压（起笔重收笔轻）

多米的长相已经定死（见 `ASSET_BRIEF.md`）：
银虎斑短毛猫，银白底毛 + 深灰虎斑纹，胸口下巴四爪纯白，
圆脸短鼻，**大而圆的浅绿色眼睛**（最强辨识特征），粉色小鼻头。

表情复用 `drawDomiFace()` 里的那套（`FACES` 表），
特别是 ω 嘴（倒 m）—— 那是这只猫的招牌。

---

## 已知的坑（都是踩过的）

**1. canvas 的 y 轴向下，画弧特别容易画反**
`jitterArc` 用 `PI→2PI` 是上半圈（∩），`0→PI` 才是下半圈（∪）。
ω 嘴曾经因此画成了正的 m，看起来像在生气。

**2. `filter` 会把整个子树合成成位图再扭曲**
子元素写 `filter: none` 完全无效。手绘边框必须做成
「伪元素承载 border/填色/滤镜并沉到 `z-index:-1`，内容留在上面」的兄弟结构。

**3. Electron 不能用 `loadFile()`**
`file://` 的 origin 是 null，不算安全上下文，`getUserMedia` 会静默失败。
现在用 `meow://` 私有协议加载（`registerSchemesAsPrivileged` 标了 `secure: true`）。

**4. Electron 二进制装不上还不报错**
`npm install` 显示成功但 `node_modules/electron/dist` 是空的。国内必须走镜像：
```bash
$env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
node node_modules/electron/install.js
```

**5. 托盘图标不是装饰**
桌宠没有任务栏图标也没有窗口边框，**托盘是退出程序的唯一入口**。
图标加载失败 = 应用关不掉。`loadTrayIcon()` 里有逐级兜底，别删。

**6. 泡泡不要旋转**
球体旋转不产生任何视觉信息，却会把烤进贴图的文字/表情转到倒立。

---

## 验收标准

- [ ] 定时到点，多米从边缘走进来，动作看起来是"走"而不是"平移"
- [ ] 吹出的每一颗泡泡都对应一段真实录音，戳破能出声
- [ ] 整个出场过程中，**鼠标在非泡泡区域点击仍然穿透到桌面**
- [ ] 出场过程可以被忽略 —— 不抢焦点、不阻塞、不弹窗
- [ ] 托盘能"跳过这次""推迟"
- [ ] 中低端笔记本上出场序列不掉帧（预渲染做到位就不会）
- [ ] 多米看起来和泡泡是同一支笔画的

---

## 不要做的事

- ❌ 不要引入 React/Vue 等框架 —— 这个项目零构建步骤是刻意的
- ❌ 不要把多米做成写实渲染 —— 和整套视觉语言冲突
- ❌ 不要每帧重跑 rough.js 或复杂路径绘制
- ❌ 不要让出场序列抢焦点或阻塞用户操作
- ❌ 不要删掉穿透判定里的 160ms 定时器
