# 猫猫声音泡泡馆 Demo 笔记

## 定位

一个私人猫声屏保 gallery：猫猫发出的 meow、purr、mrrp、chirp、半夜叫、饭盆通知不再是音频文件，而是一颗颗透明、漂浮、会漏出声音的泡泡。

## 多 Agent 分工摘要

- 产品/QA agent：把范围收敛为吹泡泡、封口保存、戳泡泡播放、筛选收藏，不做猫语翻译和宠物健康监测。
- 视觉 agent：建立古早 XP/QQ 空间感的透明泡泡、虹彩膜、网格星点、屏保漂浮氛围。
- 音频 agent：实现 MediaRecorder 录音、失败时模拟泡泡、Web Audio 模拟播放、IndexedDB 保存真实录音 Blob。
- 主集成 agent：把卡片列表改成数据驱动的泡泡 gallery，泡泡大小/光晕/漂浮速度和时长、波形、播放次数相关。

## 当前 Demo 功能

1. 首页即泡泡 gallery，内置多颗示例猫声泡泡。
2. 点击整颗泡泡播放；同一时间只允许一颗泡泡漏声音。
3. “吹泡泡”录音入口：真实麦克风优先，失败或 file:// 环境自动生成模拟泡泡。
4. 保存弹窗：泡泡名、猫咪名、声音类型、标签、备注。
5. 真实录音 Blob 保存到 IndexedDB，文字元数据保存到 localStorage。
6. 搜索、声音类型筛选、只看星点泡泡、最新/反复戳过排序。
7. 收藏后泡泡内部出现星点。
8. 响应式布局：手机端单列/大泡泡，桌面端屏保式漂浮泡泡群。

## 暂不做

- 不做猫语 AI 翻译。
- 不做公开社区、审核、举报、关系链。
- 不做复杂音频剪辑、混音、滤镜。
- 不做健康监测和医疗判断。

## 验收方式

直接打开 `index.html` 可浏览、筛选、收藏、播放模拟泡泡。需要测试真实麦克风时，建议在本目录启动 localhost 静态服务：

```bash
python -m http.server 8765 --bind 127.0.0.1
```

然后访问 `http://127.0.0.1:8765/`。
